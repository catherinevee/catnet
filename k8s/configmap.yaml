apiVersion: v1
kind: ConfigMap
metadata:
  name: catnet-config
  namespace: catnet
data:
  LOG_LEVEL: "INFO"
  WORKERS: "4"
  MAX_CONNECTIONS: "100"
  CACHE_TTL: "300"
  ENABLE_METRICS: "true"
  ENABLE_TRACING: "true"
